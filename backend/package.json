{
  "name": "notes-app-backend",
  "version": "1.0.0",
  "description": "Simple clean backend for Notes App",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "cross-env NODE_ENV=development nodemon server.js",
    "test": "mocha -r dotenv/config test/**/*.test.js --timeout 10000 --exit",
    "test:coverage": "nyc --reporter=lcov --reporter=text mocha -r dotenv/config test/**/*.test.js --timeout 10000 --exit"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^8.3.1",
    "pino": "^9.14.0",
    "pino-http": "^10.5.0",
    "pino-pretty": "^13.1.3"
  },
  "devDependencies": {
    "chai": "^4.5.0",
    "cross-env": "^10.1.0",
    "mocha": "^10.8.2",
    "nodemon": "^3.1.14",
    "nyc": "^18.0.0",
    "sinon": "^19.0.5"
  }
}
